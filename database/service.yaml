apiVersion: v1
kind: Service
metadata:
  name: postgresql-service
  namespace: attendance-system
  labels:
    app: postgresql
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: postgresql
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
      protocol: TCP
