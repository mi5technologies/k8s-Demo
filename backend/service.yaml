apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: attendance-system
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
