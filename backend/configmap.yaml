apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: attendance-system
data:
  NODE_ENV: "production"
  API_PORT: "3000"
  DB_HOST: "postgresql-service"
  DB_PORT: "5432"
  DB_NAME: "attendance_db"
